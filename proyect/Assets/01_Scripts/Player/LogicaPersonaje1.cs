using UnityEngine;

// Conservado por compatibilidad: delega en los nuevos componentes SOLID.
// Puede eliminarse una vez actualizadas las referencias en la escena.
[RequireComponent(typeof(PlayerController))]
public class LogicaPersonaje1 : MonoBehaviour
{
}
